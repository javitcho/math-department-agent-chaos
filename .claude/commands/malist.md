List all saved math agent sessions.

Run:

```bash
cd "/Users/javiermejia/Documents/GitHub Repos/math-department-agent-chaos/math-agents" && python main.py --list
```

Display the output as-is. Sessions show: session ID, topic, mode, last saved time, chunk count, and current chunk.
